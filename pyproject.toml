[project]
name = "aptitude-client"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "httpx>=0.28.0",
    "networkx>=3.2.0",
    "pydantic>=2.11.0",
    "resolvelib>=1.0.1",
    "typer>=0.16.0",
]

[project.scripts]
aptitude = "aptitude_client.cli.app:run"
