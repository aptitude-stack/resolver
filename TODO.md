# TODO

- Make winner explanation derive directly from reranker output to avoid drift.
- Replace CLI `build_kwargs: dict[str, object]` with a more typed helper or `TypedDict` to reduce call-site type drift.
